[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tpf2-mcp"
version = "0.1.0"
description = "Transport Fever 2 MCP bridge"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
